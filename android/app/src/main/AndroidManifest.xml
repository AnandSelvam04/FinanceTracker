<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Local notifications for budget/bill reminders (Android 13+). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!--
      Reads bank transaction alerts out of the SMS inbox (More > Import from
      SMS). Messages are parsed on-device and never leave it; nothing is
      posted to the ledger without explicit confirmation in the review queue.

      Play Store policy restricts READ_SMS to apps whose core function needs
      it, so declaring it here makes the app ineligible for store
      distribution. That is a deliberate trade for a sideloaded personal
      build — see the README.
    -->
    <uses-permission android:name="android.permission.READ_SMS"/>
    <!--
      another_telephony's own manifest declares ACCESS_COARSE_LOCATION for the
      cell/network-info features this app does not use. Without this override
      the manifest merger folds it into the installed app, so the permission
      list would show location access for no reason.
    -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"
        tools:node="remove"/>
    <!--
      allowBackup="false": at-rest database encryption is opt-in, so with
      Android's automatic backup enabled the default install would sync a
      plaintext copy of every transaction to the user's Google account. The app
      provides its own explicit backup/export instead. The two rules files
      cover the same ground for API 31+ and pre-31 respectively, in case this
      attribute is ever changed.
    -->
    <application
        android:label="Finance Tracker"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
