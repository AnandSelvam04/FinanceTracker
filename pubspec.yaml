name: finance_tracker
description: A new Flutter project for tracking finances.
version: 1.0.0+1
environment:
  sdk: ">=3.0.0 <4.0.0"
dependencies:
  csv: ^6.0.0
  file_picker: ^10.3.7
  fl_chart: ^1.1.1
  flutter:
    sdk: flutter
  flutter_lints: ^6.0.0
  google_sign_in: ^7.2.0
  googleapis: ^15.0.0
  googleapis_auth: ^2.0.0
  local_auth: ^3.0.0
  path: ^1.9.1
  path_provider: ^2.1.5
  provider: ^6.0.0
  shared_preferences: ^2.5.3
  sqflite: ^2.4.2
  # telephony: ^0.2.0  # Removed to avoid Play Store SMS permission issues
dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true
  assets:
    - assets/
